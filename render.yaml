services:
  - type: web
    name: health-monitor-backend
    env: python
    plan: free
    buildCommand: ""
    startCommand: "python app.py"
    workingDir: backend
    envVars:
      - key: MONGO_URI
        fromEnvVar: MONGO_URI
